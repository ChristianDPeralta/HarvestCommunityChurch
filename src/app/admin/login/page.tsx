import AdminLoginForm from '@/components/AdminLoginForm';

export default function AdminLoginPage() {
  return (
    <div className="bg-gradient-to-b from-slate-100 via-slate-50 to-white min-h-screen">
      <AdminLoginForm />
    </div>
  );
}
